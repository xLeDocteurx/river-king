import { Component } from '@angular/core';

/**
 * Minimal empty page used as a routing target in unit tests.
 * Test-only fixture: never rendered by the real application.
 */
@Component({
  selector: 'rk-app-dummy',
  standalone: true,
  templateUrl: './app-dummy.component.html',
})
export class AppDummyComponent {}
